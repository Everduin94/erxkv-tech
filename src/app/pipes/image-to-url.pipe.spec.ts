import { ImageToUrlPipe } from './image-to-url.pipe';

describe('ImageToUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageToUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
